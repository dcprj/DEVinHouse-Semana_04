const retangulo = {
    lado1 : 10,
    lado2 : 20
}

const areaRetangulo = (retangulo) => retangulo.lado1 * retangulo.lado2;

console.log (`Área do retangulo com lado 1 = ${retangulo.lado1} e lado 2 = ${retangulo.lado2} é: ${areaRetangulo(retangulo)}`);